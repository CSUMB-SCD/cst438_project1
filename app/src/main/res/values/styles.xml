<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="android:background">@color/colorPrimary</item>
    </style>

    <style name="ButtonStyle">
        <item name="android:color">@color/colorPrimaryDark</item>
        <item name="android:textColor">@color/colorAccent</item>
    </style>

    <style name="OverviewTheme" parent="Theme.AppCompat">
        <item name="android:background">@color/white</item>
    </style>

    <style name="VisualAssessmentTheme">
        <item name="android:background">@color/color4</item>
    </style>

    <style name="ListViewModelTheme">
        <item name="android:background">@color/color4</item>
    </style>
</resources>
